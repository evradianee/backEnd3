{
    "builds": [{
        "src": "chatapp/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "chatapp/wsgi.py"
        }
    ]
}